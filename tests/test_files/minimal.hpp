#include <string>

class Minimal {

    public:

        int compute(int i);
        std::string compute(std::string s);
        int compute_int(int i);
        std::string compute_str(std::string s);
};
